# 紫微知见 Vercel 免费演示版

## 部署

1. 将本目录导入 Vercel，或在目录内运行 `npx vercel`。
2. 当前版本使用本地规则生成演示解读，不需要 API Key，也不会产生模型调用费用。
3. 页面包含可随输入信息更新中心资料的十二宫示例命盘。
4. 推送代码后，Vercel 会自动重新部署。

当前命盘和解读均为交互验证用示例结构，不能当作正式排盘结果。正式上线前需要接入并测试确定性排盘引擎。
